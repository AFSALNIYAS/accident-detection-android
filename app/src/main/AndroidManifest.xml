<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Service Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- Communication Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <!-- Notification Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Other Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Hardware Features -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name=".AccidentDetectionApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Accidentdetection3">
        
        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Accidentdetection3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".login.LoginActivity"
            android:exported="false" />
        
        <!-- Register Activity -->
        <activity
            android:name=".login.RegisterActivity"
            android:exported="false" />
        
        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <!-- User Info Activity -->
        <activity
            android:name=".ui.UserInfoActivity"
            android:exported="false" />
        
        <!-- Emergency Contacts Activity -->
        <activity
            android:name=".ui.EmergencyContactsActivity"
            android:exported="false" />
        
        <!-- Speed Limit Activity -->
        <activity
            android:name=".ui.SpeedLimitActivity"
            android:exported="false" />
        
        <!-- Speed Monitoring Activity -->
        <activity
            android:name=".ui.SpeedMonitoringActivity"
            android:exported="false" />
        
        <!-- Notification Activity -->
        <activity
            android:name=".ui.NotificationActivity"
            android:exported="false" />
        
        <!-- Emergency Info Activity -->
        <activity
            android:name=".emergency.EmergencyInfoActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />
        
        <!-- Accident Alert Activity -->
        <!-- exported=true required for system to launch it via full-screen intent notification -->
        <!-- singleTop: allows onNewIntent to update location without restarting the countdown -->
        <activity
            android:name=".emergency.AccidentAlertActivity"
            android:exported="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:launchMode="singleTop" />

        <!-- Notification action receiver for I'm Safe / Send Help buttons -->
        <receiver
            android:name=".emergency.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.accident_detection3.ACTION_IM_SAFE" />
                <action android:name="com.example.accident_detection3.ACTION_SEND_HELP" />
            </intent-filter>
        </receiver>
        
        <!-- Tracking Service -->
        <service
            android:name=".service.TrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />
        
        <!-- Firebase Messaging Service -->
        <service
            android:name=".firebase.AccidentDetectionMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
            
    </application>

</manifest>